Instrucciones para usar la API:

Endpoints que devuelven JSON:
/metro/lineas -> devuelve todas las líneas de metro
/metro/linea/<linea> -> devuelve todas las paradas de la línea solicitada

Archivos que se pueden solicitar directamente:
/instrucciones.txt -> este archivo
/mapa-metro.jpg -> mapa de la red de metro